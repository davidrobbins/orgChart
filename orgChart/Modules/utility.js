/*	The getSubordinates() function can be executed from any of your project's server-side JavaScript file using the require() method as follows:	var result = require('utility').getSubordinates();	For more information, refer to http://doc.wakanda.org/Wakanda Studio0.Beta/help/Title/en/page3355.html*/exports.getSubordinates = function gs(employee) {	var myReportsCollection = ds.Employee.createEntityCollection(),		i = 0, newCollection, len;		   	myReportsCollection.add(employee.employeeCollection);   	len = myReportsCollection.length   	if (len > 0) {		myReportsCollection.forEach(function(oneReport) {			newCollection = gs(oneReport);	   		myReportsCollection.add(newCollection);	  	});	}	   	return myReportsCollection;};